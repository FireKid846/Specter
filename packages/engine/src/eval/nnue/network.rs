/// NNUE network definition — Phase 2 placeholder.
/// Will implement: input layer (768 features), hidden layer (384 neurons), output.
pub struct NNUENetwork;
impl NNUENetwork {
    pub fn evaluate(&self) -> i32 { 0 }
}
